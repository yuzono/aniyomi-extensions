ext {
    extName = 'Sudatchi'
    extClass = '.SudatchiFactory'
    extVersionCode = 20
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlist-utils"))
}
