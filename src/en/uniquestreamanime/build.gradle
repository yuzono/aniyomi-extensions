ext {
    extName = 'UniqueStream (Anime)'
    extClass = '.UniqueStreamAnime'
    extVersionCode = 1
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlist-utils"))
}
