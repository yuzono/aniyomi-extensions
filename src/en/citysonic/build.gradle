ext {
    extName = 'CitySonic'
    extClass = '.CitySonic'
    themePkg = 'dopeflix'
    baseUrl = 'https://sflix2.to'
    overrideVersionCode = 0
}

apply from: "$rootDir/common.gradle"

dependencies {
    api(project(":lib:videostr-extractor"))
}
